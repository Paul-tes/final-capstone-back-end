databases:
  - name: roomic
    databaseName: roomic_gzhz
    user: roomic_gzhz_user

services:
  - type: web
    name: roomic
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: roomic_gzhz
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false